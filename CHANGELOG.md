# Changelog

## 2026-07-14 (v49) — 🧹 v1.4.7: чистка подсказок в переводах + тап-перевод слов во фразах
- По запросу Ивана (4 пункта):
- ✅ **Чистка переводов wordbank (63 поля, 929 слов).** Бэкап: `.claude/archive/wordbank_backup_20260714.json` (433 КБ). Правила: R1 — скобки «(от …)» удалены (14); R2 — скобки-уточнения, содержащие само слово/его форму («принимать (кабыл алуу)» → «принимать»), удалены (44); R3 — скобки с «X» в кавычках, делящим корень со словом («(деепр. от «кароо»)»), удалены. 6 случаев «форма «X»» вне скобок получили ручные глоссы без раскрытия основы (болот→«будет», боло→«быть (служебная форма)», болуш→«бытие (отглагольная форма)», келе→«приходя…», кете→«уходя…», коё→«кладя, пуская…») — LLM-глоссы, флаг needs native check уже в status. Отдельно починен «тилдүү» → «-язычный» (дефис). Скобки без подсказки сохранены («Бишкек (столица КР)», «с, вместе с (послелог)»).
- ✅ **Проверка (п.3):** пустых переводов 0; битых символов 0; дублей shortTr 22→28 (новые — легитимные синонимы: каза/өлүм «смерть», ар/сайын «каждый» и т.п., движок дистракторов исключает их через similarMeaning). Симуляция `distractors()` реальным кодом app_nav на чищеных данных: 1858 наборов (оба направления × 929 слов), 0 совпадений с ответом, 0 пропусков similarMeaning, все наборы полные. RTDB: `set wordbank/words` (929 ключей, spot-check ок), `curated=2026-07-14`; schema НЕ бампнута (иначе сброс doneLessons); `content/words.json` синхронизирован (63 поля). Клиенты подтянут переводы при следующей онлайн-загрузке автоматически.
- ✅ **Тап по слову во фразах/предложениях (п.4).** `kyTap`/`kyLookup` в app_core: слова кыргызской фразы в pintro/pky2ru/plis(тихий) размечаются; кликабельны (пунктир) только найденные в банке — точное совпадение или самая длинная основа-префикс (≥3 букв, хвост ≤6). Попап в стиле tipbox: слово + 🔊 + перевод, при неточном совпадении приписка «≈ по основе слова»; закрывается тапом мимо/повторным тапом/сменой экрана (`render`). Тест на 454 фразах: 1360/2321 слов кликабельны (59%), 845 точных, битого HTML 0.
- Файлы: `app_core.js?v=3`, `app_session.js?v=4`, `app_boot.js?v=3` (build-коммент), `index.html` (CSS `.kyw`/`#kypop`, бампы). Деплой через git-клон (маунт по-прежнему отдаёт обрезанные js).
- ⚠️ Известное ограничение: основа-префикс может изредка дать ложный корень (напр. «балдар» → «бал»); в UI это явно помечено «≈ по основе слова».

## 2026-07-12 (v48) — 🧭 v1.4.6: умное «Продолжить» + свободное «Повторить» в «Моих словах»
- По запросу Ивана, две функции:
- ✅ **«Продолжить» ведёт в раздел последнего пройденного урока.** Новое состояние `lastPos={lvl,uid,d}` (kv `lastpos` + RTDB-sync в `progress`): пишется в `lessonScreen`/`pLessonScreen` (уровень фраз — по принадлежности юнита level2/level3, не по UI). При старте вкладка Слова/Фразы/Предложения восстанавливается (`loadMeta`, merge в `syncPull` по дате). Цель кнопки — `contTarget`/`contTargetP` (app_nav): недоделанные уроки от последнего юнита вперёд (включая его тему), иначе fallback `firstUndone`/`firstUndoneP`. Дерево на главной раскрывается к этой же цели.
- ✅ **«🔁 Повторить» в «Моих словах»** — свободная тренировка: случайные 7 слов (или 5 фраз) из текущего фильтра (все/учу/знаю/сложные + поиск + вкладка). `practiceScreen` (app_session) → `finishSession(..., practice=true)`: SRS **не трогается** (нет `srsOnResult`/записи в srs-store — due/step/seen не меняются, дни повторения не сдвигаются); XP/стрик/цель дня/квесты начисляются как обычно; на финише — «Повторение пройдено!». Подпись под кнопкой предупреждает, что циклы не меняются.
- Файлы: `app_core.js?v=2` (lastPos в loadMeta, `saveLastPos`), `app_nav.js?v=2` (contTarget/contTargetP, кнопка+обработчик в wordsScreen), `app_session.js?v=3` (saveLastPos в уроках, practiceScreen, practice-режим finishSession), `app_boot.js?v=2` (lastPos в encProg/syncPull, build-коммент), `index.html` (бампы ?v).
- Проверка: `node --check` всех файлов; смоук-тест contTarget/contTargetP на фикстурах (7/7 pass: fallback без lastPos, продолжение юнита, добитый юнит → вперёд/fallback, фразы, чужая вкладка). Маунт снова отдавал обрезанные файлы → деплой через git-клон в `~` с реприменением правок скриптом (рецепт HANDOVER), правки в клоне и в папке проекта идентичны.

## 2026-07-12 (v47) — 🎯 v1.4.5: «Соедини пары» — русский слева, кыргызский справа
- Проблема (отзыв Ивана): в упражнении match кнопки KY и RU шли двумя блоками подряд и в 2-колоночной сетке перемешивались по колонкам.
- ✅ `app_session.js`: рендер чередует пары — в каждой строке RU слева, KY справа (порядок внутри колонок по-прежнему перемешан, соответствий по строкам нет).
- ✅ `index.html`: `app_session.js?v=2`; `sw.js`: CACHE → `kyapp-shell-v3`.
- Деплой: push.py (3 файла), live проверен через cache-buster — v=2 и новый рендер на месте.

## 2026-07-07 (v46) — 🧹 аудит документов: проект подготовлен к передаче (Opus-ready)
- Проблема: `HANDOVER.md` сильно устарел (v1.3.5, нить OmniVoice «открыта», index.html описан монолитом, правило «правки только питон-патчем»); «Current state» в `CLAUDE.md` разросся до ~15 КБ истории; в корне 5 закрытых handover/report-файлов; `src/README.md` = «пока пусто».
- ✅ `CLAUDE.md` переписан: компактный Current state (v1.4.4, RTDB-узлы, разбитая оболочка, пайплайн новых слов, Next), правила дополнены (бамп `?v=N`, дубли top-level, no loanwords, флаги native check), указатели на HANDOVER/CHANGELOG. История не потеряна — она в CHANGELOG.
- ✅ `HANDOVER.md` переписан как единая точка входа новой сессии: актуальное состояние, инструменты, ловушки песочницы (обновлены: файлы src/ мелкие → Edit безопасен; обход больших файлов через git-клон в `~`), приёмы, Next, карта архива.
- ✅ В `.claude/archive/` перенесены закрытые: `HANDOVER_omnivoice_revoice.md`, `HANDOVER_opus_audio_integration.md`, `HANDOVER_course_and_app_v01.md`, `REPORT_omnivoice_2026-07-07.md`, `HF_TOKEN_HOWTO.md` (токен уже в `_quickref.md`). Рецепт точечной озвучки через Space остаётся доступен в архивном revoice-handover (ссылки из CLAUDE/HANDOVER ведут туда).
- ✅ `src/README.md` актуализирован (7 скриптов, правила правок). Сверено: index.html 21 330 Б, 7 `<script src>` в правильном порядке, tracker-бэкапов ровно 2, ключи только в `_quickref.md`.
- Код и RTDB не тронуты, деплой не требовался.

## 2026-07-07 (v45) — 📦 v1.4.4: этап 2 разбивки — основной скрипт порезан на 6 файлов
- Карта: 25 секционных комментариев `/* ---------- */`; top-level исполняемый код только в core (`firebase.initializeApp`, 2 document-listener'а) и boot (async IIFE запуска в самом конце) — резать безопасно.
- Разрез по границам секций, **реконструкция байт-в-байт** (assert): `app_core.js` (ядро: IndexedDB/состояние/аудио/уровни/стрик/SRS/RTDB/маскот/экраны, 17 КБ) · `app_demo.js` (3.5 КБ) · `app_nav.js` (навигация/упражнения/фразы/Мои слова, 25 КБ) · `app_extras.js` (Чтение/Диалоги/квесты/грамм-подсказки, 17 КБ) · `app_session.js` (урок/повторение/сессия, 17 КБ) · `app_boot.js` (конфетти/модалки/sync/запуск + история build-комментариев, 17 КБ).
- Каждый файл: classic-скрипт (НЕ ES-модуль), свой `'use strict';` (директива изначально была на весь скрипт — семантика сохранена), заголовок с описанием. **index.html = 21 330 байт** (было 116 754, изначально 318 900). Порядок тегов = порядок кусков, все с `?v=1`. `<!-- build -->`-маркер в head; история build — в app_boot.js.
- sw.js SHELL += 6 файлов. `node --check` каждого куска OK (границы не режут конструкции). Дублей top-level объявлений между файлами НЕТ.
- ✅ Открытие: все 63 `onclick` — рантайм-присваивания `el.onclick=()=>…` (не HTML-атрибуты) → страх ES-модулей был преувеличен; но и выгоды они не дадут, остаёмся на classic.
- Live: build success, все 9 файлов = клон байт-в-байт, `</html>` на месте. src/ синхронизирован.
- ⚠️ Правило впредь: правка любого app_*.js → бамп его `?v=N` в index.html.

## 2026-07-07 (v44) — 📦 v1.4.3: этап 1 разбивки оболочки — DEMO_AUDIO вынесен из index.html
- Идея E (одобрена Иваном): разбить index.html по файлам, пока Fable доступен; этап 1 = вынос данных.
- Разведка: `DEMO_AUDIO` = ОДНА строка 202 605 симв. = 63% файла. DEMO_WORDS/UNITS/GRAMMAR_TIPS — по паре КБ, не трогали.
- Вынос: `demo_audio.js` (обычный `<script src>` ДО основного скрипта, НЕ ES-модуль — top-level const виден следующим classic-скриптам; никаких module-ловушек). Тег с `?v=1`; sw.js network-first + `ignoreSearch:true`, в SHELL добавлен `./demo_audio.js` (офлайн-прекэш).
- **index.html: 318 900 → 116 754 байта (−63%)** — ниже порога проблем маунта (~100+ КБ, но втрое меньше прежнего).
- Вся работа в git-клоне в `~` (обход маунта по memory-рецепту): правки python-скриптом, `node --check` обоих JS (main script извлечён — OK), целостность данных (18 слов, голоса m/f, 198 КБ), пуш напрямую.
- Live проверен: raw = клон байт-в-байт; Pages build success; `index.html?v=$RANDOM` = 116 754 + `</html>`, demo_audio.js 202 864, тег на месте.
- Рабочая копия `src/` синхронизирована из клона (index.html, sw.js, + demo_audio.js). Заодно вылечен стухший inode (маунт показывал 315 892).
- Next: этапы 2+ (вынос модулей srs/audio/demo) — риски расписаны Ивану, ждём отмашки.

## 2026-07-07 (v44) — 🐹 друг: подключён Уланбек (saparulanbek@)
- Письмо от saparulanbek@gmail.com (тема «KY APP»). RTDB `allowed/` += `saparulanbek@gmail,com: true`. Проверено `get allowed` — 3 ключа.
- Ответ-инструкция (ИИ-ассистент, 5 режимов) — черновиком в Gmail.
- Второй запрос (abdulqodirmuhammad03@, Teach for Uzbekistan) — НЕ подключён по решению Ивана; Иван ответил сам про демо/полный доступ.

## 2026-07-07 (v43) — 🐹 друг: подключён Даниил Романовский
- Письмо от romanowskiy@gmail.com (тема «KY APP»): «подключи меня».
- RTDB `allowed/` += `romanowskiy@gmail,com: true` (точки→запятые, как в rules). Проверено `get allowed` — 2 ключа (Иван + Даниил).
- Ответ-инструкция (по FRIENDS_HOWTO) создан черновиком в Gmail — ждёт отправки Иваном.

## 2026-07-07 (v42) — 🐛 ФИКС повторения (v1.4.2): идеальная сессия наконец двигает срок
- 💬 **Иван**: «прошёл всё на 100%, но слова из повторения не удалились, снова они же».
- 🔎 **Причина (реальный баг, не UX):** в `runSession` объект `errors` (он же `results`) наполнялся ТОЛЬКО при ошибках (строка `errors[curR()]=...` в finishTask). `finishSession` обновляет SRS в цикле `for(...results)` — т.е. только по словам с ошибкой. Новые слова спасал `errors[t.r]=0` в обработчиках `intro`/`pintro`. Но в **повторении** intro-заданий нет (`withIntro=false`) → ответил всё верно → `results={}` → `srsOnResult` НЕ вызывается → `due`/`step` не двигаются → слова возвращаются каждый раз.
- ✅ Подтверждено на данных Ивана: все 29 слов застряли на `step 0/-1`, продвинулись ТОЛЬКО те, где были ошибки (r9/r29/r12/r32 — lapses≥1). Симуляция после фикса: 10/10 верных → все уходят в будущий `due` (0 остаётся).
- ✅ **Фикс:** `runSession` засеивает `errors[r]=0` для ВСЕХ изучаемых рангов сессии (слова + фразы + match) до начала. Идемпотентно, старые intro/pintro-сиды оставлены.
- ✅ **«Мои слова» — пометки цикла (запрос Ивана):** бейдж на каждой строке 🟢 длинный цикл (step≥4) / 🔵 закрепляю (step 2–3) / 🟠 учу / 🌀 трудное + под баром ⏰ «пора повторить» (due≤сегодня, красным) либо «через N дн.»; в деталях по тапу добавлен `цикл N дн.`. Легенда обновлена. CSS `.cyb`/`.cynote`, хелпер `cycleInfo`, бар расширен 44→80px, `.wst` min-width 92.
- ⚠️ Деплой: маунт снова держал устаревший inode index.html (mtime до правок) — правки видны только файловым инструментам; пуш сделан после синка/сверки live.
- Застрявшие 29 слов чинятся сами при следующем верном повторении (миграция данных не нужна).

## 2026-07-07 (v41) — 🏁 переозвучка ЗАКРЫТА: Иван подтвердил, старое аудио удалено
- 💬 **Иван**: «демо звучит хорошо, можешь удалять» — гейт пройден.
- ✅ Перед удалением сверено: live-код читает только `audio2` (в коде осталось 1 упоминание audio/w|p — устаревший комментарий, не обращение); `audio/d` читается диалогами → **правило `audio` в rules ОСТАВЛЕНО** (пункт хандовера «удалить правило вместе с узлом» не выполнен сознательно — под ним живёт audio/d).
- ✅ RTDB: `audio/w` и `audio/p` удалены; `audio` = только {d, meta}; audio2 цел (929/454). Откат теперь только из архива (wav) + конверсия заново.
- ✅ `out.zip` → `.claude/archive/out_omnivoice_kenenbek_20260707.zip` (cmp IDENTICAL, 136 МБ), оригинал из корня удалён (allow_cowork_file_delete).
- Нить OmniVoice полностью закрыта: v1.4.1 live, 2 голоса, демо озвучено, доки в актуальном состоянии.

## 2026-07-07 (v40) — INSTRUCTIONS.md обеих connect-папок переписаны (уроки проекта)
- 🔎 Обнаружено: `github-connect/INSTRUCTIONS.md` был ОБРЕЗАН на полуслове (1447 Б, раздел v36 про truncated-push сам стал жертвой этой же проблемы записи через маунт).
- ✅ `github-connect/INSTRUCTIONS.md` переписан целиком: восстановлен раздел truncated-push guard + добавлены: правило «переживающие сессию файлы — только в папку проекта» (инцидент rules_backup_v5), деплой-флейк Pages/.nojekyll/ретриггер, работа с ДРУГИМИ репо через PAT (создание приватных, инвайт коллабораторов, git из ~ т.к. git на маунте не работает, rm на маунте в &&-цепочке), приватный репо для шаринга непубликуемого контента, факты песочницы (45 с, смерть фоновых, `&` фонит всю цепочку).
- ✅ `firebase-connect/INSTRUCTIONS.md` дополнен: бэкап rules ПЕРЕД rules-set только в папку проекта + верификация после; миграция через параллельный узел (audio→audio2, откат, удалять старое только с подтверждения); meta/ver для инвалидации клиентских кэшей; префикс r у числовых ключей (массивы!); bulk-чанки 100–150 ключей + резюм-манифест + сверка counts + round-trip проверка; base64-аудио паттерн; факты песочницы.
- Обе папки самодостаточны для копирования в другие проекты.
 — независимая приёмка v1.4.0/v1.4.1 (Fable): ПРИНЯТО
- ✅ Все проверяемые утверждения отчёта Opus подтверждены независимо: live index.html 315892 Б, `</html>`, build v1.4.1; RTDB `audio2/w`=929 (11 узлов = {cv}, все 11 рангов сверены), `audio2/p`=454, meta ver=2 + history; старые `audio/w`+`audio/p` целы (откат возможен); rules v6 содержит audio2; спот-чек-страница live (200, 3176588 Б); mp3 из RTDB = mono 32k, пики −3.5 dB, длительности сходятся с манифестом kenenbek'а; `pickVoice` из live-кода прогнан в node: 10000 выборок ≈ 49/51, cv-приоритет и null-safe ок, инвалидация строкового кэша в коде есть; DEMO_AUDIO = 18 записей (~197 КБ), «16»/«2131» = {cv}, остальные {m,f}; «о приложении» упоминает OmniVoice/2 голоса.
- 🔧 Единственный пробел: `rules_backup_v5.json` в проекте не найден (остался в эфемерной песочнице той сессии) → сохранён `firebase-connect/rules_backup_v6_20260707.json` (v5 = v6 минус правило audio2).
- Гейт прежний: слушает Иван → потом удаление audio/w+p и архивация out.zip.

## 2026-07-07 (v39, app v1.4.1) — 🐹 демо озвучено
- 💬 Иван: «а в демо?» → раньше демо было немым (анонимам RTDB-аудио закрыто + принудительный тихий режим). Теперь OmniVoice Apache-2.0 + CV CC0 → аудио можно вшить в публичную оболочку.
- ✅ **DEMO_AUDIO** (~198 КБ base64) — 18 демо-слов: 16 OmniVoice ×2 голоса + 2 живых CV-клипа (жок r16, ооба r2131). `getAudio` в демо читает из памяти (`if(S.demo)return DEMO_AUDIO[r]`), не из RTDB. Тихий режим в демо больше не форсится (`S.mute=false`), плашка «звука нет» и спец-обработка кнопки 🔇 убраны — голоса чередуются так же случайно. index.html 113→316 КБ (грузится один раз). node --check OK, live v1.4.1 сверен. Правка через temp→cat (защита от обрезки маунтом).

## 2026-07-07 (v38, app v1.4.0) — 🔊 переозвучка на OmniVoice LIVE (два голоса)
- ✅ **Спот-чек-страница** (10 слов + 3 фразы, оба голоса, сырые WAV) собрана и по просьбе Ивана опубликована: https://ninenko.github.io/ky-app/spotcheck_omnivoice_voices.html (OmniVoice Apache-2.0 + CV CC0 → публиковать можно). Иван дал добро («опубликуй … потом остальное»).
- ✅ **Своя сверка out.zip**: 2766/2766, id-множество = 929 слов + 454 фразы × {m,f} (0 missing/extra), manifest совпал с ffmpeg (dbfs=max_volume), тишины/брака нет.
- ✅ **wav → mp3** 32k mono, **пик-нормализация к −3 dBFS** (gain=min(0,−3−dbfs); тихие клипы не усиливаются). 2766 файлов, выборочно проверено (пики ≈ −3.5 dB).
- ✅ **RTDB `audio2`** (новый узел, старый `audio` не тронут): `audio2/w/r<rank>`={m,f} (11 CV-слов={cv}, перенесены из audio/w), `audio2/p/r<id>`={m,f}, `audio2/meta` ver2 (+history старого meta). Залито base64-чанками; счётчики: w=929, p=454. **Rules v5→v6** (+audio2 read по тому же allowlist; бэкап rules_backup_v5.json).
- ✅ **App v1.4.0** (питон-патч src/index.html): пути `audio/*`→`audio2/*`; `getAudio` тянет объект голосов, `pickVoice` = случайный m/f при каждом плее (cv → единственный вариант); **инвалидация старого AkylAI-кэша** (строковый кэш в IndexedDB → перезагрузка объекта); «о приложении» обновлён (OmniVoice, два голоса). node --check OK, смоук pickVoice (≈50/50) OK, live v1.4.0 сверен (размер, build-tag, audio2, `</html>`).
- ⏳ **Ждёт Ивана**: подтвердить, что в приложении звучит корректно и голоса чередуются → тогда удалить старые `audio/w`+`audio/p` (audio/d диалоги и meta.history не трогаем) и заархивировать out.zip.
- 📜 Лицензия: OmniVoice Apache-2.0 + референсы CV CC0 → аудио свободно и вне личного пользования (снят блокер AkylAI CC-BY-NC).

## 2026-07-07 (v37) — out.zip от kenenbek принят; хандовер интеграции для Opus
- ✅ `out.zip` (корень, 136 МБ) проверен: **2766/2766 wav** (929 слов + 454 фразы × м/ж), problems.json пуст, manifest полный, dbfs −18.2…−0.0 (часть под 0 → в план заложена пик-нормализация к −3 dBFS), длительности 0.52–13.62 с, всё с 1-й попытки. ⚠️ Sample-этап был пропущен — голоса Иван ещё НЕ слушал → в план вставлен спот-чек до заливки.
- 💬 **Решение Ивана: голоса в приложении чередуются случайно** при каждом воспроизведении (привыкание к разным произношениям).
- ✅ `HANDOVER_opus_audio_integration.md` — пошаговый план для Opus: распаковка чанками → спот-чек Ивана → mp3 32k mono с нормализацией → новый узел `audio2` ({m,f}; 11 CV-слов = {cv}) + rules v6 → патч app (случайный голос, пер-голос кэш + инвалидация по meta/ver, префетч обоих, «о приложении») → после подтверждения удалить старое audio/w+p → доки. Чеклист приёмки внутри. Учитывать инцидент v35/v36: перед пушем index.html — wc -c/tail, guard в push.py не обходить.

## 2026-07-07 (v36) — 🛡 защита от пуша обрезанных файлов (по следам инцидента v35)
- 💬 **Иван**: прописать инструкции, чтобы такое не повторялось.
- ✅ `push.py`: встроенный integrity-guard — блокирует пуш (1) `.html` без `</html>` в последних 200 байтах, (2) любого файла, усохшего >5% против копии в репо; `--force` для осознанного обхода. Протестировано: обрезанный html блокируется, целый проходит.
- ✅ `github-connect/INSTRUCTIONS.md`: раздел «Truncated-push protection» — что делать при отказе guard'а (подождать 5–10 с / проверить wc -c+tail / обход через temp в outputs), запрет `--force` «чтобы прошло», обязательная сверка live после пуша index.html (кэш-бастер, размер, `</html>`).
- ✅ `CLAUDE.md` Rules: короткое правило про медленный синк маунта и guard.
- Тех: сам push.py при правке тоже обрезался маунтом — восстановлен через тот же обход (temp в outputs → cat).

## 2026-07-07 (v35, app v1.3.7-fix) — 🔥 хотфикс: приложение не открывалось после v34
- 💬 **Иван**: приложение не открывается, проверить работу Опуса (v34).
- 🔍 Диагноз: логика правки v1.3.7 верна, но при пуше `index.html` обрезался (111 455 байт вместо ~111 963, обрыв посреди build-комментария, без `</script></body></html>`) — известный глюк синка больших файлов (CLAUDE.md «Тех»). Опус не сверил `wc -c`/tail перед пушем.
- ✅ Восстановление: целая v1.3.6 из истории git (3b9a448) + та же однострочная правка `reviewRanks(){ return dueRanks(); }` + build-комментарий v1.3.7. Синтаксис проверен node, хвост на месте, 111 963 байта. Запушено, live проверен (размер+правка совпадают).

## 2026-07-07 (v34, app v1.3.7) — повторение: правильно повторённое сегодня уходит в длинный цикл
- 💬 **Иван**: слова, которые сегодня уже повторил правильно, должны уходить в более длинный цикл.
- ✅ `reviewRanks()` теперь = только просроченные по `due` (`dueRanks()`). Убрано включение всего, что трогал сегодня (`ls===today`). Правильный ответ уже ставит будущий `due` (srsOnResult) → слово выпадает из очереди и идёт в свой интервал; неправильный ставит `due=today` → остаётся для доработки в тот же день. Счётчик «Повторение» на главной обнуляется, когда все просроченные закрыты. Задеплоено (v1.3.7).

## 2026-07-07 (v33) — генерация уходит на GPU друга (kenenbek); ДВА голоса
- 💬 **Иван**: квоты HF слишком тесные; друг с большой GPU (github: kenenbek) прогонит OmniVoice локально. **Решение: два голоса в приложении — мужской и женский.** (Снимает блокер v32.1.)
- ✅ Приватный репо **ninenko/ky-app-audio**, kenenbek приглашён (write): README-интро (по-русски), `generate.py` (929 слов + 454 фразы × 2 голоса = 2766 клипов; --sample = 10 слов на подтверждение Ивану ДО массовой генерации; проверка тишины ≤−28 dBFS + длительности, до 3 ретраев с ростом duration; резюм по out/manifest.json; брак → out/problems.json), `items.jsonl` (id: w_r<rank>/p_r<id>, NFC), `ref/` — референсы из CV CC0.
- 🎙 Референсы: муж. common_voice_ky_19476697 (спикер 3065 клипов, twenties), жен. common_voice_ky_18450897 (1535 клипов, twenties); тексты дословно в generate.py. Финальное слово за Иваном после --sample.
- ⏭ Когда придёт out.zip: конверт в mp3 32k mono → base64 → RTDB. **Схема audio меняется** (2 голоса): спроектировать (напр. audio/w/r<rank> = {m,f}) + патч index.html (переключатель/чередование) + инвалидация IndexedDB-кэша. Дополнён HANDOVER_omnivoice_revoice.md.
- Тех: git init на маунте не работает — пуш из ~; инвайт: PUT /repos/../collaborators/<user>.

## 2026-07-07 (v32.1) — переозвучка OmniVoice: пайплайн проверен, БЛОКЕР по квоте ZeroGPU
- ✅ Пайплайн проверен end-to-end: gradio_client + Space `k2-fsa/OmniVoice` работает с токеном (env готов, ffmpeg). Клип «көп» (M-референс) — 1.4с, max −6.4 dB, чисто.
- ✅ Отобраны референсы из CV validated.tsv (30741 строк, 271 диктор): M=fa40d7e9 (3065 клипов, `common_voice_ky_17995999`), F1=9606abdf (1535, `..18452680`), F2=2bb5ae40 (1182, `..18454072`) + старый тест-референс `..17589356`. Клипы + ref_text сохранены в `content/audio/omnivoice_ref/` (candidates.json).
- ⛔ **БЛОКЕР: free ZeroGPU quota = 3.5 мин/день** — кончилась после 2 генераций (көп, кийин, M-голос). Для 1383 клипов free = недели-месяцы. Решение Ивану: **HF PRO $9/мес (25 мин/день, ×8; +credits $1/10мин)** vs free-трикл vs локальный CPU. Ждём выбора Ивана + «оба голоса» (M и F) на спот-чек до массовой генерации.
- Сгенерённое сохранено: `content/audio/omnivoice_ref/test_clips/` (M_көп, M_кийин).

## 2026-07-07 (v32) — решение: переозвучка на OmniVoice; A/B-страница остаётся как демо
- 💬 **Иван**: «OmniVoice намного лучше» → переделать всю озвучку; страницу оставить для демонстрации.
- ✅ `HANDOVER_omnivoice_revoice.md` — полная инструкция переозвучки для нового чата (рецепт генерации, проверка тишины/длительности КАЖДОГО клипа, выбор единого референса + спот-чек Ивана до массовой генерации, резюм-манифест на маунте, чанки в RTDB, квота ZeroGPU и опция PRO, инвалидация IndexedDB-кэша аудио, audio/meta, «о приложении», лицензии).
- ✅ A/B-страница: добавлена атрибуция источников (AkylAI CC-BY-NC некоммерч. демо / OmniVoice Apache-2.0 / CV CC0), перезалита. Решение Ивана: НЕ удалять.
- ✅ Доки обновлены: CLAUDE.md Current state, HANDOVER.md (нить закрыта → ссылка на revoice-хандовер).
- Не трогать при переозвучке: 11 живых CV-клипов, audio/d (Peace Corps), структуру ключей RTDB.

## 2026-07-06 (v31) — A/B-набор полный (7 слов), live
- ✅ HF-токен Ивана получен → `_quickref.md` (## Hugging Face). С токеном (env HF_TOKEN / Client(..., token=)) квота ZeroGPU не мешала.
- ✅ Перегенерены/догенерены эми, көп, жакшы, күн, үй (du=1.0–1.4). **Проверка каждого клипа на тишину** (`ffmpeg volumedetect`, порог max −28 dB, норма −8…−16 dB) — по требованию Ивана; «эми» изначально было тишиной, «жакшы» поймано и перегенерено ретраем.
- ✅ `ab_test_omnivoice.html` пересобрана (7 пар + референс, preload=none) и задеплоена: https://ninenko.github.io/ky-app/ab_test_omnivoice.html (проверено, 8 строк). ⚠️ Временная, удалить после проверки (`push.py --delete`).
- HANDOVER.md переписан (v1.3.5 + нить OmniVoice), HF_TOKEN_HOWTO.md создан.

## 2026-07-06 (v30) — A/B-тест OmniVoice vs AkylAI (частично)
- ✅ Через HF Space k2-fsa/OmniVoice (gradio_client, voice cloning, lang=Kirghiz) сгенерированы кийин/эми/жаңы; референс — носитель из CV-корпуса (common_voice_ky_17589356, CC0). Сравнение: `ab_test_omnivoice.html` (в корне, аудио base64, НЕ пушить в репо).
- ⚠️ Аноним. квота ZeroGPU кончилась после 3 слов; көп/жакшы/күн/үй в очереди. Ждём HF-токен Ивана (бесплатный акк) → в `_quickref.md`.
- Тех: фоновые процессы в песочнице умирают с концом bash-вызова (bwrap --die-with-parent) — локальная установка torch/omnivoice невозможна одним вызовом; путь через Space рабочий и быстрый (~10 с/слово). Клипы AkylAI выдернуты из RTDB `audio/w/r<rank>` (ranks 25/29/38/50/148/199/326).

## 2026-07-06 (v29) — research: OmniVoice как кандидат на замену AkylAI TTS
- 💬 **Иван**: почему AkylAI, а не OmniVoice? + «кийин» звучит не как «к».
- 🔎 OmniVoice (k2-fsa / Next-gen Kaldi, апрель 2026): zero-shot TTS на 646 языков, кыргызский есть (ky/kir, ~47 ч в трейне), **Apache-2.0** (свободнее, чем CC-BY-NC у AkylAI), voice cloning по 3–10 с референса → можно клонировать голос носителя из Common Voice CC0. Кандидат на A/B-тест озвучки; ничего не менялось.
- 📝 Про «кийин»: в кыргызском к перед и/е/ө/ү — мягкий [к] (как рус. «кь»), перед а/о/у/ы — глубокий [къ]; мягкость в «кийин» скорее норма, но качество клипа не проверено на слух.

## 2026-07-06 (v29) — финальный Суур с кубком (app v1.3.6)
- 💬 **Итерации с Иваном локально** (5 рендеров без деплоя, cairosvg-превью): кубок в левой руке → рука соразмерна правой → строго горизонтальна → кубок крупнее → целиком левее головы, обе ручки видны.
- ✅ Финал: рука = линия от края тела (зеркально правой, stroke 12 round), лапа на постаменте, чаша 30px с бликом; viewBox расширен до «-14 0 134 134» (иначе левая ручка клипалась). Техника: marmotCup = marmot() + замена viewBox + подмена arm-r + врезка кубка. Рендер из index.html сверен с утверждённым (diff — только пробелы).

## 2026-07-06 (v28) — кубок крупнее, в правой лапе у пояса (app v1.3.5)
- 💬 **Иван**: кубок у головы мелкий, «лапа внизу осталась» → кубок больше, лапу к кубку, место — над прогресс-баром.
- ✅ Обе лапы в обычной позе; большой кубок (чаша 28px в координатах маскота) на уровне пояса справа, поверх — лапа, держащая край чаши. Превью 200px и 52px проверены визуально. Деплой с ретриггером.

## 2026-07-06 (v27) — кубок в поднятой правой лапе (app v1.3.4)
- 💬 **Иван**: кубок на карточке юнита не видно (перекрывал прогресс-бар) → «в правую руку, над прогресс-баром» (сначала сказал в левую, тут же поправил на правую).
- ✅ marmotCup: лапа arm-l (у зрителя справа) повёрнута вверх (rotate 180 вокруг плеча), кубок уменьшен и посажен в неё на уровне головы. Превью отрендерено (cairosvg) и проверено визуально.

## 2026-07-06 (v26) — Суур с кубком + повторение дня (app v1.3.3)
- 💬 **Запросы Ивана**: (1) на карточке пройденного юнита не хватает Суура — «может Суур с кубком?»; (2) слова, пройденные сегодня, должны сразу попадать в «Повторение».
- ✅ marmotCup(): маскот с золотым кубком (SVG-врезка перед </svg> маскота) вместо 🏆 во всех 5 списках юнитов (слова/категории, фразы, чтение, диалоги). Нюанс: медаль в коде — одинарные кавычки внутри ternary в template literal → интерполяция ${} не работает, сделано конкатенацией. Node-смоук marmotCup OK.
- ✅ Повторение: srsOnResult пишет `ls` (последний показ); reviewRanks() = просроченные + всё тронутое сегодня (свежие в конце очереди — сортировка по lapses/due не менялась). Кнопка и сессия используют reviewRanks (в демо — свой список, как было). srs.ls синкается вместе с srs.

## 2026-07-06 (v25) — демо: письма и повторение (app v1.3.1–v1.3.2)
- 💬 **Запросы Ивана**: (1) в модалке писем Суура — пометка про демо; (2) вернуть «Повторение» в демо.
- ✅ v1.3.1: модалка писем в демо — плашка от Суура «письма не работают: я же не запоминаю ни прогресс, ни адрес», галочка disabled, «Сохранить» отвечает тостом.
- ✅ v1.3.2: кнопка «Повторение» в демо не появлялась (SRS ставит срок «завтра», а демо живёт один день) → в демо повторение доступно по всем изученным словам (seen>0), приоритет проблемных сохраняется.

## 2026-07-06 (v24) — 🐹 Демо-вход без регистрации (app v1.3)
- 💬 **Запрос Ивана**: открытый демо-вход без почты и без сохранения, чтобы друзья могли попробовать; кнопка «Демо-вход» + модалка в стиле Суура.
- ⚖️ Ограничение: RTDB-контент открывать анонимам нельзя (Исаев ©, Belebele CC-BY-SA, TTS CC-BY-NC — приватно). Демо = встроенный мини-курс ТОЛЬКО из открытых данных: 18 реальных записей банка (словарь Tilchi, open-source), 3 юнита («Знакомство», «Мир вокруг», «Первые слова») × 2 урока.
- ✅ Реализация: кнопка «🐹 Демо-вход» на экране логина → модалка Суура («прогресс запоминать не буду… напиши Ивану на ivan.ninenko@gmail.com, тема KY APP») → startDemo(): S.demo, тихий режим принудительно (тумблер отвечает тостом), kvGet/kvSet отключены (всё в памяти, IndexedDB не трогаем), фразы/чтение/диалоги скрыты (нет pcourse → нет табов), баннер-gtip на главной с mailto, «выйти» = reload. XP/комбо/квесты/подсказки работают в памяти.
- 🧪 Верификация: node-смоук (18 слов → у каждого 3 дистрактора, 3 юнита/6 уроков), node --check, live байт-в-байт. Патч v13_patch.py (7 якорей). Деплой: снова флейк+ретриггер.

## 2026-07-06 (v23) — экран «нет доступа» от Суура (app v1.2.10)
- 💬 **Запрос Ивана**: вместо сухого «Permission denied» — приветствие от Суура с объяснением и почтой.
- ✅ При ошибке contента с permission denied (и пустом кэше): маскот (marmot 140 wave) + «Салам! Я Суур… Иван должен включить тебя в список пользователей, твоего адреса (email) я там не нашёл. Напиши Ивану на ivan.ninenko@gmail.com с темой „KY APP"» (mailto-ссылка с subject). Прочие ошибки — прежний generic-текст. Патч v1210_patch.py.
- Деплой: шаг Deploy падал 2 раза подряд (build ok) — подтверждено через API jobs, это флейк GitHub; второй ретриггер прошёл.

## 2026-07-06 (v22) — Enter-навигация, дата сгорания заморозки, примечание в письмах (app v1.2.6–v1.2.8)
- 💬 **Запросы Ивана**: (1) Enter на десктопе = «Дальше» после верно/неверно; (2) когда сгорела заморозка — писать в подсказке; (3) в модалке писем — что для подключения надо сообщить Ивану адрес.
- ✅ v1.2.6: глобальный keydown Enter жмёт «Дальше» (фидбек урока #fnext, интро #ok, чтение/диалоги, финиш .finish #cont); из инпутов/селектов и при фокусе на кнопке не перехватывается (у type-упражнения свой Enter на проверку — конфликта нет, input к моменту фидбека disabled).
- ✅ v1.2.7: при сгорании заморозки пишем `streak.burn` (дата) и `burnN` (сколько); подсказка ❄️ показывает «Последний раз заморозка сгорела DD.MM — был пропуск» или «Пока ни одна не сгорала (на этом устройстве)». Ретроактивно дату не восстановить — раньше не записывалась.
- ✅ v1.2.8: модалка «✉️ Письма Суура» — P.S. про доступ по списку. **v1.2.9: откат по решению Ивана** (не нужен: письма включаются сами, рассылка идёт всем progress/* с reminders=true — проверено по src/gas/Code.gs; инструкция для друзей и так есть в FRIENDS_HOWTO).
- Деплой: v1.2.5 и v1.2.8 понадобились ретриггеры Pages (флейк повторяется — уже 4 раза за день).

## 2026-07-06 (v21) — точность на финише урока (app v1.2.5)
- 💬 **Баг от Ивана**: точность иногда 0%, хотя были верные ответы.
- 🔎 Причина: acc считалась как доля СЛОВ без единой ошибки (correct/total по results); если в уроке 2–3 слова и в каждом хоть раз ошибся — 0%, даже при куче верных ответов (ошибочное задание повторяется до успеха).
- ✅ Теперь: в runSession счётчик всех ответов (`sess={ans,ok}`, включая match как один ответ) → finishSession показывает верные/все ответы. XP-формула не менялась (по-прежнему за слова без ошибок). Патч v125_patch.py (9 якорей); деплой снова флейкнул → ретриггер.

## 2026-07-06 (v20) — фавиконка: мордочка Суура (app v1.2.4)
- 💬 **Запрос Ивана**: иконка для вкладки браузера — мордочка Суура.
- ✅ `<link rel="icon">` с SVG data-URI (голова маскота из marmot(), viewBox 13 -5 94 94, base64); превью 64px проверено (рендер cairosvg). Патч v124_patch.py.
- ✅ **PWA-иконки тоже**: icon-192/512.png перерисованы — мордочка Суура на фирменном красном #d7263d (cairosvg, безопасная зона maskable; исходник outputs/icon_marmot.svg, копии в src/). Live сверен побайтово. Manifest не менялся (те же имена). На телефоне иконка обновится при переустановке ярлыка («В начальный экран» заново); index.html не трогали.

## 2026-07-06 (v19) — футер + подсказка про уровни (app v1.2.2–v1.2.3)
- 💬 **Запросы Ивана**: (1) отформатировать футер, убрать деталь про источники фраз; (2) подсказка про уровни (Окуучу и т.д.) по клику.
- ✅ v1.2.2: футер в 3 строки — цель дня / ссылки flex-рядом (💡 Грамматика · ✉️ Письма Суура · ℹ️ О приложении · Выйти) / краткий дисклеймер (без «Фразы: Исаев/Шнейдман, Tatoeba»).
- ✅ v1.2.3: строка уровня на главной кликабельна (+ⓘ) → tipbox со всеми 7 уровнями (Баштоочу 0 → Манасчы 6000 XP), позицией «← ты» и пояснением: пороги нелинейные, уровень = усердие, знание меряют SRS-повторения.
- Ответ на вопрос Ивана про счётчики: «Мои слова» = все изученные слова (seen>0, только слова), «Повторение» = слова+фразы с подошедшим сроком SRS — числа и не должны совпадать. Патчи v122/v123, деплой v1.2.2 понадобился ретриггер Pages.

## 2026-07-06 (v18) — 🔇 Тихий режим (app v1.2.1)
- 💬 **Запрос Ивана**: переключатель на главной, чтобы заниматься без звука (громкое место, лекция), потом включать обратно.
- ✅ Тумблер 🔊/🔇 в статбаре главной (kv `mute`, локально). В тихом режиме: playAudio/playDialog глушатся (нет автоплея и 🔊 везде), в новые уроки не добавляются `lis`/`plis`; если переключить посреди урока — «Что ты услышал?» деградирует в текстовый вопрос (слово/фраза показываются); «Диалоги» проходятся по транскрипту (текст вместо 🔊, финиш без кнопки звука). Все 5 уровней проходимы без аудио.
- Патч `v121_patch.py` (12 якорей), node --check OK.

## 2026-07-06 (v17) — 🎧 Диалоги (Peace Corps) + квесты недели + грамм-подсказки (app v1.2)
- 🎧 **Уровень «Диалоги»** (tracker #9–10, public domain): из 6 mp3 Peace Corps нарезано 10 диалог-клипов носителей (2.5–15.6 с; приветствия / знакомство / общение). Пайплайн: faster-whisper tiny (ASR английской наррации → якоря) → ffmpeg + silenceremove → ASR-верификация каждого клипа (без англ. наррации; d3/d8 перерезаны по словным таймстампам). Транскрипты — дословно из KY_Kyrgyz_Language_Lessons.pdf, 17 MC-вопросов на русском (Claude). `content/build_dialogs.py` → RTDB `dialogs` + `audio/d/<id>` (base64 mp3 32k mono, ~385 КБ).
- ✅ **App v1.2**: 5-й таб «🎧 Диалоги» (lvltabs теперь 3+2 с переносом) — юниты→клипы, урок = 🔊 автоплей + переслушивание + MC-вопросы, транскрипт в финише; doneLessons `gd*` переживают смену контента; кэш клипов в IndexedDB (audio store, ключи `dlg:*`).
- 🎯 **Квесты недели** (playbook): 150 XP / 10 уроков / 3 идеальных, бонусы +20/+20/+25 XP по кнопке; сброс по ISO-неделе; карточка на главной; счётчики локальные (kv `week`, не синкается). Подтверждено Иваном.
- 💡 **Грамм-подсказки** (playbook, Babbel-style): 14 кратких заметок (гармония гласных, -сың/-сыз, -бы?, -дан, эмес, SOV…; примеры — только аттестованные формы из курса/Peace Corps); ротация на финише каждого урока (kv `tipn`) + экран «💡 Грамматика» из футера.
- 🔐 **Rules v5**: + `dialogs` read по allowlist (бэкап v4 сделан; подтверждено Иваном).
- Патч `v12_patch.py` (16 якорей), node --check OK, смоук-тест квестов/подсказок в node OK. Tracker: Peace Corps audio + Basic Interactive Lessons → Processed (Manual и Competencies остаются кандидатами).

## 2026-07-06 (v16) — UX прогресса: бары, подсветка начатых, 🏆 (app v1.1.1)
- 💬 **Запрос Ивана**: прогресс-бар у каждого раздела, фоновая подсветка начатых, радующий маркер за пройденные.
- ✅ Все 4 списка юнитов (категории слов, юниты слов, фразы, чтение): мини-прогресс-бар везде (юнитам слов добавлен — раньше был только у категорий); класс `go` (начат: 0<готово<всего) — мягкая зелёная карточка; класс `fin` (пройден) — **золотая карточка + золотой бар + 🏆 с pop-анимацией**. Патч v111_patch.py (6 якорей), node --check, live 82787 байт (build v1.1.1).
- ⚠ Деплой: шаг «Deploy to GitHub Pages» флейкнул (build success, deploy failure — это у GitHub, не у нас; .nojekyll не при чём) → лечится пустяковым коммитом-ретриггером (.nojekyll с таймстампом). Занесено в HANDOVER.

## 2026-07-06 (v15) — v1.1: аудирование + уровень «Чтение» (Belebele)
- 💬 **Иван**: «забей на ударение»; запрос — аудирование по слову/фразе (слышу, не вижу текст → выбираю значение → появляется написание); «сделай по максимуму».
- ✅ **Аудирование**: типы `lis` (слова, после match) и `plis` (фразы, перед pbuild) — большая кнопка 🔊 (replay по тапу), автоплей, 4 варианта перевода (те же дистракторы v2 + confus-трекинг), **написание кыр+перевод в фидбеке всегда** (не только при ошибке). Урок длиннее на 1 задание на слово/фразу; в повторение попадает автоматически (buildTasks/buildPTasks общие).
- ✅ **Уровень «Чтение»**: Belebele kir_Cyrl (facebook/belebele, CC-BY-SA — только приватная RTDB) = 488 текстов / 900 MC-вопросов на кыргызском; `content/build_reading.py` → reading.json (49 юнитов по 10 текстов, сортировка по длине текста от простых к сложным, ассерты) → RTDB `reading` (~0.8 МБ). **Rules v4**: +узел reading (allowlist-паттерн). App: 4-й таб «📚 Чтение» → отдельный экран (ленивая загрузка + кэш в idb kv, сетка уроков 5×, ✓ пройденным), урок = текст (карточка, скролл) + вопросы по одному, XP=10+2·верный+5 за perfect, стрик/цель дня через registerActivity, финиш-экран → назад в «Чтение». doneLessons ключи `rb<idx>`; при смене контента (migrateDone) `rb*` сохраняются — SRS для чтения нет.
- ✅ Патч v11_patch.py (13 якорей), node --check, конвейеры прогнаны в node на реальных данных (intro,ky2ru×3,match,lis×3,ru2ky×3,type×3 / pintro,pky2ru×3,plis×3,pbuild×3). Live 82062 байт, build v1.1. «О приложении» обновлено (4 уровня, аудирование).
- ⚠→✅ **Pages-билд v1.1 упал** (Jekyll, причина не видна без auth; `{{`/`{%` в файле нет) → добавлен `.nojekyll` в корень репо (Pages теперь чистая статика) + перепуш → build success, live v1.1 сверен. Нюанс: свежесозданный файл на маунте может читаться пустым — push.py сам скажет «EMPTY (stale mount?)», пересоздать и повторить.
- ✅ HANDOVER.md переписан под v1.1 (ловушки песочницы: питон-патчи, фон умирает, curl -C - для torch; пайплайн добавления слов с аудио).

## 2026-07-06 (v14) — 🎉 v1.0: полная озвучка (AkylAI TTS + Common Voice)
- 💬 **Ивана**: native speaker посмотрела — «норм»; совет: усилить ударение на последний слог. Решение: делаем аудио и объявляем v1.
- ✅ **TTS**: `kyrgyz-ai/TTS_small` (MMS-VITS, дообучен AkylAI на 4 ч кыргызской речи, 1 диктор, CC-BY-NC — личное использование, только в приватной RTDB). Прямой ввод кириллицы, ө/ү/ң ок. Кандидаты MMS-tts-kir (без дообучения) и OmniVoice (tracker #13) отклонены в пользу кыргызско-специфичной модели.
- ✅ **Пайплайн** (`content/audio/gen_all.py`): 929 слов + 454 фразы → mp3 mono 32k (~10 МБ, 5–7 КБ/клип); 11 слов с точными однословными клипами Common Voice (CC0) — живые записи вместо TTS; seed 42; ffprobe-выборка — длительности 0.6–7 с, битых нет; чанк-генерация (санбокс убивает фон, бюджет 33 с/вызов, ~50 прогонов субагентом). Манифест: `content/audio/audio_manifest.json`.
- ✅ **RTDB**: `audio/w/r<rank>`, `audio/p/r<id>` = base64 mp3 (929+454 ключей сверено, спот-чек магии mp3); `audio/meta` — источники. **Rules v3**: + узел `audio` read по allowlist (тот же паттерн; применено, rules-get сверен).
- ✅ **App v1.0 live** (73267 байт, побайтово): IndexedDB v2 (+store audio), getAudio/playAudio (RTDB → кэш idb → data:URI), префетч всего урока на старте, делегированный клик (capture+stopPropagation — 🔊 в «Моих словах» не раскрывает строку). Озвучка: intro/pintro (🔊+автоплей), ky2ru/pky2ru (🔊 у вопроса+автоплей), ru2ky (после ответа), type/pbuild (после проверки), match (при угаданной паре), «Мои слова» (🔊 в строке). «О приложении»: абзац про синтез + подсказка «ударение почти всегда на последний слог» (совет native speaker; сам VITS ударением не управляет — надо послушать сэмплы).
- ✅ Патч audio_patch.py (16 якорей с assert), node --check. Tracker: строка AkylAI TTS (Processed), CV-заметка; бэкап 20260706 (старый 0704c удалён).
- 📌 Стек в песочнице: torch 2.12 cpu (колесо докачивалось curl -C -, фон между вызовами не живёт), transformers 5.13.

## 2026-07-06 (v13) — Аудит проекта + экран «Мои слова» (app v0.3.5-words)
- 💬 **Запрос Ивана**: аудит — можно ли назвать v1? + опция посмотреть изученные слова.
- ✅ **Аудит**: контент целостный (929 слов / 55 юнитов / 330 уроков, 454 фразы / 46 юнитов / 160 уроков — обе стороны join без потерь, проверено скриптом), app live, tracker/бэкапы в норме. **Решение Ивана: v1 объявляем только вместе с аудио** (пока это «v0.9»); аудио — следующий приоритет. Мусор на заметку: postanovlenie_654 PDF в корне (не из проекта?), 4 words_backup_*.json в content/.
- ✅ **App v0.3.5-words live** (70769 байт, побайтово сверено): кнопка «📖 Мои слова · N» на главной (под «Повторение», ghost-стиль) → экран изученного словаря: табы Слова/Фразы (с числами), фильтры-чипы Все/Учу/Знаю/Сложные (💪 step≥4, 📗 остальные, 🌀 leech: ≥3 ошибок и >⅓ показов — та же формула, что leech-cap), живой поиск ky+ru без перерендера (data-s + hidden, клавиатура не прыгает), сортировка алфавитная, полоска прочности (step/7, у сложных красная), тап по строке — детали (полный перевод, тема, видел/ошибок/дата повтора). Патч mywords_patch.py (5 якорей с assert), node --check, логика прогнана в node на реальных content/*.json (60 слов+10 фраз мок-SRS, фантомный id отброшен).

## 2026-07-06 (v12) — UX: фидбек-плашка под вопросом (app v0.3.4-fb)
- 💬 **Запрос Ивана**: «Азамат!» с Сууром появлялся снизу экрана — приходилось тянуться пальцем от вариантов ответа.
- ✅ `.fb` из position:fixed внизу → инлайновая карточка, вставляется в `#ex` сразу под вариантами; scrollIntoView(nearest) для длинных упражнений; padfb 170→40px; анимация slideup→fbpop. Патч fb_patch.py (3 якоря), node --check, live сверен (65075 байт, v0.3.4-fb).

## 2026-07-06 (v11) — App v0.3.3-about: «О приложении», письма Суура для всех, подготовка к друзьям
- 💬 **Запрос Ивана**: поделиться приложением с друзьями; инфо-экран про то, как построили; галочка подключения почтовых напоминаний.
- ✅ **App v0.3.3-about live** (64986 байт, побайтово сверено): футер → «о приложении» (модалка: история Иван+Claude, открытые источники Tilchi/RFE-RL/CV/Tatoeba/Исаев, предупреждение про ИИ-переводы) и «✉️ письма Суура» (галочка + email, префилл из Google-аккаунта, валидация) → `progress/<uid>/settings {email, reminders}`; settings в encProg/syncPull (локальное приоритетнее). Патч about_patch.py (9 якорей с assert), node --check, push.
- ✅ **Apps Script → мультипользовательский**: users_() сканирует progress/* по settings.reminders+email (владелец — fallback всегда); dailyCheck/weeklyReport на каждого подписчика, LAST_WEEK_<uid> per-user, Telegram только владельцу. Квота MailApp 100/день — на друзей хватает. Тест-режим (ALWAYS_MAIL) удалён. Зеркало src/gas/Code.gs обновлено.
- ✅ **FRIENDS_HOWTO.md** — инструкция для друзей (вход, Add to Home Screen, включение писем, честное предупреждение про ИИ-переводы).
- ✅ **Правила RTDB v2 ПРИМЕНЕНЫ** (ок Ивана, 2026-07-06): allowlist `allowed/<email с , вместо .>` (управляет Иван/Claude), контент read для allowlisted, progress/$uid — только своему uid. Иван посеян в allowed до применения (без локаута). Добавить друга: `python3 firebase-connect/db.py set "allowed/imya,drug@gmail,com" 'true'`.
- ⚠️ Копирайт-нюанс зафиксирован: фразы Исаева/Шнейдмана и переводы — личное использование; узкий круг друзей — решение Ивана, для широкой публикации нужна замена на открытые данные (правило в CLAUDE.md).

## 2026-07-06 (v10) — Исследование: напоминания/отчёты без Claude
- 💬 **Запрос Ивана**: письма в 20:00 если не учился + недельный отчёт. Отправка почты из Claude невозможна (Gmail-коннектор только черновики; scheduled tasks работают лишь при открытом приложении).
- ✅ Deep-research (5 веток): `research/notifications_research.md`. Итог: Firebase Functions — только Blaze (отпало); Web Push — сложно/ненадёжно; SendGrid/Mailgun free tier закрыты. **Рекомендация: Google Apps Script** (бесплатный cron ±15 мин, GmailApp шлёт настоящие письма самому себе, RTDB через REST, может слать и в Telegram). Альтернатива: GitHub Actions + Telegram-бот (задержки до 60 мин, автоотключение через 60 дн. тишины).
- ✅ **Apps Script «Ky App Reminders» развёрнут** (Claude через Chrome, авторизацию кликнул Иван): ежедневно ~20:00 dailyCheck (streak.last/doneToday из RTDB → письмо-напоминание, если не учился), вс ~20:30 weeklyReport (XP/уроки/слова/повторения с дельтами, снапшот в ScriptProperties), TZ Asia/Bishkek. Копия кода: `src/gas/Code.gs` + `appsscript.json` (5 скоупов, RTDB через getOAuthToken владельца). Опция Telegram заложена (TG_TOKEN/TG_CHAT_ID в Script Properties).
- ✅ **Тест-режим включён** (ALWAYS_MAIL=1): письмо «молодец» приходит даже если занимался; выключение — Run → disableTestMode. dailyCheck прогнан вручную — тестовое письмо отправлено. ⚠️ Побочно: 2 ранних ручных прогона dailyCheck (до setup) могли прислать дубли напоминания.
- ✅ **Недельный отчёт v2 «живой»**: рассказ от Суура вместо списка (стартовая точка/дельты, % курса через shallow-подсчёт wordbank+phrases, ветки по стрику и повторениям, «Кийинки жумага чейин!»); тестовый отчёт отправлен Ивану, снапшот LAST_WEEK зафиксирован (следующее вс покажет дельты с 07-06).
- ✅ **Тексты Суура v2 (Duolingo-стиль)**: 4 случайных шаблона напоминаний (стрик под угрозой / багаж N слов / «бир-эки-үч» / пассивно-агрессивное «напоминания не работают»), число слов из srs через shallow-запрос; недельный отчёт тоже от Суура. Иван проверил тест-письма (скрин) — **disableTestMode выполнен**, тест-режим снят. src/gas/Code.gs синхронизирован.

## 2026-07-05 (v9) — Word bank v1.4: тема «Педагогика» из sources/Pedagogy (40 слов)
- 💬 **Решение Ивана** (AskUserQuestion): Pedagogy-PDF → доменная лексика; обрабатываем до аудио.
- ✅ **Пайплайн**: pdftotext 6 PDF (473K слов, текстовый слой — OCR не нужен) → частотный словарь → кандидаты: есть в Tilchi KR как заглавное слово, нет в банке, НЕ русское заглавное слово (фильтр по tilchi_RK.tsv) → 302 кандидата → ручная курация Claude: отброшены русизмы, прошедшие фильтр («класс», «том», «образ»), производные от русских корней («практикалык», «психологиялык» и т.п. — прозрачны для Ивана), словоформы («балдар», «муну»), офф-домен фольклор («кылыч», «баатыр»).
- ✅ **40 слов** (ranks 3040–3079, топ по частоте корпуса): тарбиялоо, сынчыл, оозеки, адабият, талдоо, «ой жүгүртүү» (коллокация), чыгармачылык, корутунду, ыкма, маани, түшүнүк, тыянак, эреже, милдет… + закрыт пробел банка: **биринчи** («первый» отсутствовал!). Переводы курированы по Tilchi (raw-глоссы сохранены в translation_raw), все «needs native check», NFC проверен.
- ✅ **Course**: build_course.py TOPICS + «Педагогика» → пересборка: 55 юнитов (Педагогика I–III), 330 уроков, 929 слов. RTDB: wordbank + course перезалиты, спот-чек rank 3051 и meta — ок. Приложение подхватит без деплоя (join по freq_rank).
- ✅ tracker: 3 строки Pedagogy → Processed. Бэкап words_backup_pre_pedagogy.json.

## 2026-07-05 (v8) — Аудит проекта + интейк sources/Pedagogy
- 💬 **Запрос Ивана**: добавил 6 PDF в sources/Pedagogy, «изучи и проведи полный аудит».
- ✅ **Интейк Pedagogy** (все с текстовым слоем, OCR не нужен): ОшМУ Жарчысы 2021 Т.2 №4 (журнал, 1317 стр., ky+ru статьи); 4 магистерские диссертации МУЦА Токмок 2026 (проектное обучение/география, речевые стереотипы учителя, цифровой сторителлинг, критическое мышление/история); Типтүү программа «Педагогика» (КГУ им. Арабаева, 2022, 80 стр.). Все — академический кыргызский C1+, НЕ материал для базового курса. Занесены в tracker (3 строки, Status=New). Оценка: лучший кандидат на извлечение доменной лексики — типовая программа; ограничение — обилие русизмов (правило «без заимствований»).
- ✅ **Аудит**: банк 889 слов (835 Tilchi + 29 реформа + 15 freq + 10 школьные) — CLAUDE.md говорил «860», исправлено; phrases.json 454 пары, pcourse 46 юнитов — ок; app v0.3.2-srs live = src/index.html (60570 байт) — ок; tracker: Жусаев в Processing (отложен legitimно), остальное консистентно.
- ✅ Мусорный файл `sources/{%.png}.txt` (OCR-фрагмент предисловия Исаева) → `.claude/archive/isaev_intro_ocr_fragment.txt`. Бэкап tracker_backup_20260705.xlsx (старый 0704b удалён, лимит 2).
- ⏳ От Ивана (всё ещё): обновить приложение на всех устройствах для активации sync.

## 2026-07-05 (v7) — SRS-тюнинг: пиявки и приоритет повторения (app v0.3.2-srs)
- 💬 **Вопрос Ивана**: учитывается ли «эти слова знаю — показывать редко, в этих ошибаюсь — держать в активном повторении»? Ответ: да, SM-2-lite (1→3→7→16→35→80→180 дней; ошибка = ступень −2 и due сегодня). Но найдены и закрыты 2 слабости:
- ✅ **Leech-cap**: слово с ≥3 ошибками и долей ошибок >1/3 показов не уходит дальше 16 дней, пока доля не разбавится успехами (тест: пиявка держится 0-2-6-15 и выходит на 34-79-179 только после серии верных ответов; здоровое слово — как раньше).
- ✅ **Приоритет повторения**: вместо случайного порядка — сначала слова с наибольшим числом ошибок, затем самые просроченные; слов за сессию 7 (было 5) + 3 фразы.
- ✅ Пуш index.html 60570 байт, live сверен (v0.3.2-srs).
- ⚙️ Тех: синк опять рвал файл на маунте — восстановление детерминированной цепочкой в один bash-вызов: cat index_sync3 (полная база v0.3-levels) → sync_patch.py → srs_patch.py → node --check → пуш. Патчи с assert'ами лежат в outputs/.

## 2026-07-05 (v6) — v0.2 sync: прогресс в RTDB (app v0.3.1-sync)
- 💬 **Запрос Ивана**: «запускай v0.2 sync прогресса в RTDB».
- ✅ **Sync local-first**: узел `progress/<uid>` (rules уже покрывают — только email Ивана, не менялись). На входе `syncPull()`: pull → merge → запись слияния обратно; после каждого урока/смены цели `syncPush()` (fire-and-forget, оффлайн молча игнорируется — локальный прогресс главный).
- ✅ **Merge-правила**: SRS по-ключево (больший seen, при равных поздний due); xp = max; стрик по последней дате (при равных max count); doneLessons = объединение; confus = max по паре; doneToday = max при совпадении дня. Числовые ключи srs/confus в снапшоте с префиксом `r` — иначе RTDB превращает объект в разреженный массив на 20K+ элементов.
- ✅ Тест merge-логики в Node с моками firebase/kv: 9 кейсов (локальный новее / удалённый новее / новое слово / объединения) — все ок. Тост «☁️ Прогресс синхронизирован» при реальном слиянии.
- ✅ Пуш index.html 59820 байт, live сверен (v0.3.1-sync).
- ⚙️ Тех: синк Windows→маунт снова рвал файл (56926 торн-стейт) — восстановлен cat'ом полного index_sync3 + python-патч sync_patch.py с assert'ами на уникальность замен; обе копии сведены.
- ⏳ От Ивана: обновить приложение на всех устройствах; после первого входа прогресс сольётся в облако, дальше телефон/комп будут видеть общий прогресс.

## 2026-07-05 (v5) — Три уровня: слова / фразы / предложения (app v0.3-levels)
- 💬 **Запрос Ивана**: «сделай три уровня: слова (есть), простые фразы, сложные предложения»; загрузил 2 учебника в sources/.
- ✅ **Источники**: kyrgyz_tili_isaev.pdf (самоучитель Исаев/Шнейдман 1988, 145 стр. скан) — золотая жила: параллельные диалоги «ky — ru», «запомните выражения», пословицы. kyrgyz_tili_Jusaev.pdf (323 стр.) — OCR-выборка 33 стр. показала: параллельных пар НЕТ (тексты ky + русские задания), отложен (потенциал: глоссы уроков, словарь ~15 стр.). Оба PDF — сканы без текстового слоя; OCR tesseract kir+rus (tessdata_fast), 145 стр. Исаева распознаны полностью (3 параллельных субагента; ~5 битых страниц пере-OCRены).
- ✅ **Пайплайн извлечения** (`content/extract_pairs.py` → `apply_review.py` → `build_phrases.py`): абзацы «ky — ru» → фильтры (транскрипции, словарные списки, мусор) → определение языка сторон по словарному банку+морфологии (39 пар с перепутанными сторонами перевёрнуты) → **автопочинка OCR-повреждений ө/ү/ң** по корпусному словнику RFE/RL 818K токенов (32 токена) → **полная вычитка Claude всех 429 пар** (~60 правок русских OCR-опечаток и битых ky-букв, 23 пары отброшены: обрезанные/склеенные/несовпадающие) → NFC. Спот-чек по изображению стр. 44: все пары совпадают с книгой.
- ✅ **content/phrases.json = 454 пары** (397 Исаев + 57 Tatoeba CC-BY): id 20001+, уровень 2 (2–4 слова, 244 шт.) / уровень 3 (5+, 210 шт.), 13 тем (кейворд-классификатор), статус «needs native check». **pcourse.json**: L2 = 24 юнита/85 уроков, L3 = 22 юнита/75 уроков, по 3 фразы на урок, темы по порядку книги.
- ✅ **App v0.3-levels live**: табы уровней 🔤 Слова / 💬 Фразы / 📜 Предложения; юниты-аккордеоны с темами; упражнения: карточка фразы → MC-перевод (дистракторы: та же тема+длина+adaptive confus, фильтр двусмысленности) → **сборка фразы из плиток** (слова фразы + 2–3 ложные из той же темы, без свободного ввода — принцип MC из benchmark takeaways из-за агглютинативной морфологии); фразы >9 слов — только MC. SRS общий (id фраз 20001+ не пересекаются с ranks слов), повторение смешанное (5 слов + 3 фразы), migrateDone учитывает фразовые уроки. Тесты на реальных данных: pdistract 100% тематических, 0 дублей; pTiles 0 пунктуации/дублей в ложных плитках.
- ✅ RTDB: phrases + pcourse залиты (спот-чек №20454 ок). Пуш index.html 56926 байт, live сверен побайтово. tracker.xlsx: +2 строки Sources (Исаев Processed, Жусаев Processing/отложен), Tatoeba → Processed.
- ✅ Учтено из benchmark_ky_takeaways: NFC везде, MC/плитки вместо ввода текста, гармония гласных как флаг при вычитке (ложные срабатывания на заимствованиях — использована только как сигнал).
- ⚠️ Советская лексика учебника 1988 (колхоз, пионеры и т.п.) оставлена как есть — аутентичный учебный материал; сказать Ивану, если мешает — можно отфильтровать тему/страницы.
- ⚙️ Тех: синк снова обрезал index_sync3.html на маунте (мид-комментарий) — починено доливкой хвоста printf >>; правило прежнее: после каждого cat сверять wc -c и node --check. OCR-артефакты в outputs/ocr/ (png не удаляются из песочницы — можно почистить руками).
- ⏳ От Ивана: обновить приложение (тост «Контент обновлён», прогресс слов не тронут), потыкать вкладки Фразы/Предложения.

## 2026-07-05 (v4) — Выжимка из проекта «Benchmark Ky» в research/
- 💬 **Запрос Ивана**: проверить проект «Benchmark Ky», взять полезное для приложения.
- ✅ Создан `research/benchmark_ky_takeaways.md`: новые источники (Belebele kir_Cyrl ~900 MC-вопросов на чтение, XL-Sum Kyrgyz, KyrgyzLLM-Bench, TUMLU ky, UD Kyrgyz-KTMU) + принципы (MC вместо свободного ввода из-за морфологии; NFC-нормализация Ң/Ү/Ө в words.json и проверке ответов; гармония гласных как валидатор словника; LLM-кыргызскому не доверять без носителя). В `Benchmark Ky/My_Python/` — harness для сравнения LLM по кыргызскому (пригодится при выборе модели для контента).
- ⚠️ `Benchmark Ky/files.zip` повреждён.

## 2026-07-05 (v3) — Заимствования убраны из банка
- 💬 **Решение Ивана**: заимствования из русского не учить — «я их и так знаю». Правило на будущее: слова, совпадающие с русскими, в банк не добавлять.
- ✅ Удалены 8 слов (аттестация, мониторинг, стандарт, концепция, предмет, класс, план, квалификация). Word bank **v1.3 = 889 слов** (терминов реформы осталось 29, ranks 3011–3039). Курс пересобран: 53 юнита, 316 уроков; «Образование и культура IV» = 16 слов. RTDB перезалита, tracker поправлен (−8 строк, заметка в Sources).

## 2026-07-05 (v2) — Термины школьной реформы из пост. №654 (word bank v1.3, course +2 юнита)
- 💬 **Запрос Ивана**: из postanovlenie_654_30.10.2024.pdf (Концепция перехода на 12-летнее образование, параллельный ky-ru) взять термины «для раздела школа» — это его рабочая лексика.
- ⚠️ Документ как текст-источник забракован: OCR-слой битый (ө/ү/ң → ц/у/латиница), лексика канцелярская. Но как источник ТЕРМИНОВ использован: отобраны частотные в документе термины реформы, **написание выверено по Tilchi/kaikki (НЕ по OCR)**, переводы — параллельный русский текст + Tilchi (raw сохранён в translation_raw).
- ✅ **+37 терминов** (freq_rank 3011–3047 синтетические), topic «Образование и культура», type «term (школьная реформа)»: билим берүү, окутуу, баалоо, тарбия, көндүм, сабаттуулук, компетенттүүлүк, натыйжа, деңгээл, мазмун, сапат, санариптик, каржылоо, кесиптик, профилдик, башталгыч, бүтүрүүчү, боюнча, окуу жылы/китеби/планы, мектепке чейинки, ишке ашыруу, камсыз кылуу, иштеп чыгуу, кайра даярдоо, жетишкендик, муктаждык, жөндөм + 8 заимствований (аттестация, мониторинг, стандарт, концепция, предмет, класс, план, квалификация). Впервые в банке многословные единицы — дистракторы v2 подбирают по числу слов, ок. Word bank **v1.3 = 897 слов**; бэкап words_backup_pre_reform.json.
- ✅ Курс пересобран: 53 юнита (+2: «Образование и культура» теперь I–IV), 318 уроков. RTDB: wordbank+course перезалиты (спот-чек №890 ок). tracker.xlsx: +37 строк Word bank, +1 строка Sources (пост. №654, Processed); бэкап tracker_backup_20260705.xlsx (⚠ удалить 20260704b вручную — песочница не даёт удалять, теперь бэкапов 3).
- ⚙️ Тех: синк снова обрезал файлы (outputs/add_reform_terms.py и content/build_course.py) — чинил доливкой хвоста через cat >>; обе стороны сверены (wc -c, grep).

## 2026-07-05 — Дистракторы v2 (app v0.2-ui+dist2)
- 💬 **Фидбек Ивана**: слова так учить нравится; главное улучшение — дистракторы из той же тематической области.
- ✅ **Дистракторы v2** (по best practices MCQ: семантически близкие дистракторы эффективнее случайных — Susanti et al. 2018 и др.): 1) **≥1 из той же темы** гарантированно + тематическим бонус в скоринге (часто 2–3 из темы); 2) **adaptive**: неверно выбранный вариант запоминается (`confus` в IndexedDB) и чаще возвращается дистрактором именно к этому слову; 3) **фильтр двусмысленности**: кандидаты с общим значимым токеном/корнем перевода исключаются — не бывает двух верных вариантов; 4) сохранены подбор по длине/числу слов и ≥1 изученное. Кнопки вариантов теперь несут data-r (freq_rank) — проверка ответа по rank, а не по тексту.
- ✅ Верификация на words.json+course.json (Node, 1720 кейсов): 100% с тематическим дистрактором, 100% с изученным, 0 утечек двусмысленности; adaptive-слово возвращается в ~55% прогонов после 1 ошибки.
- ✅ Пуш index.html 45972 байта, live сверен (метка dist2). RTDB не менялась (контент тот же).
- ⚙️ Тех: синк src/index.html снова завис (маунт отдавал старые 43722 байта, обрезан на середине) — обход прежний: полный файл → outputs/index_sync2.html → cat поверх → node --check → пуш.

## 2026-07-04 (v4) — Курс v2: тематические категории; +10 школьных слов; app v0.2-ui+nav2
- 💬 **Вопрос Ивана «почему время+календарь и государство+политика вместе?»**: старый курс резал слова частотными ярусами, название юнита = 2 крупнейшие темы среза (новостной корпус → политика везде). Решение Ивана: «сделай отдельные категории, можно меньше шагов».
- ✅ **Курс v2 (тематический)**: build_course.py переписан — юнит = одна тема (крупные разбиты I/II/…, хвост <8 слов вливается в предыдущий), внутри темы слова по частотности, категории упорядочены по самому частому слову темы; уроки по 2–3 слова. Итог: 19 категорий, 51 юнит, 305 уроков, 860 слов. schema «course v2 thematic».
- ✅ **Школьная лексика (запрос Ивана)**: +10 слов из словаря Tilchi в «Образование и культура» — китеп, сынып, тапшырма, университет, дептер, калем, парта, экзамен, окутуучу, каникул. Переводы откурированы из словарных статей (raw сохранён), freq_rank 3001–3010 синтетические (нет в частотнике), статус «added (school topic); cleaned (LLM); needs native check». Word bank v1.2 = 860 слов; бэкап words_backup_pre_school.json. Уже были: мектеп, сабак, мугалим, окуучу, студент, билим, жооп, суроо, сынак.
- ✅ **App v0.2-ui+nav2**: навигация = категории с иконками (19, эмодзи из «надёжного» набора) → юниты I/II/… → дорожка уроков; категория из одного юнита раскрывается сразу в дорожку (меньше шагов — ок по решению Ивана, правило ≤7 ослаблено на верхнем уровне). **Миграция прогресса**: id уроков сменились → при смене контента doneLessons сбрасывается и пересчитывается из SRS (урок пройден, если все его слова изучались); SRS/XP/стрик не тронуты.
- ✅ RTDB: wordbank v1.2 + course v2 перезалиты (спот-чек meta и слова №860 ок). Пуш index.html 43722 байта, live сверен. tracker.xlsx: +10 строк Word bank; бэкап c, старейший бэкап удалён (держим 2).
- ⏳ От Ивана: обновить приложение; при следующем входе появится тост «Контент обновлён» и прогресс пересчитается.

## 2026-07-04 (v3) — v0.2-ui+nav1: подсказки статбара, дерево навигации, честные дистракторы
- ✅ **Всплывающие подсказки** (запрос Ивана) — только для иконок статбара: тап по 🔥/❄️/⚡/кольцу цели показывает тёмную плашку с объяснением механики (динамические значения: текущий стрик, запас заморозок, XP до следующего уровня, прогресс цели). Повторный тап или тап по плашке закрывает. Больше нигде подсказок нет (указание Ивана).
- ✅ **Навигация-дерево** (было 50 юнитов одной лентой = 300 нодов): правило Ивана «≤7 вариантов на шаг» → 2 части («Негиз», «Өсүш») × 5 этапов × 5 юнитов × 6 уроков; все ветвления ≤7, все разделы сохранены. Реализовано аккордеоном на главной: часть → этап → юнит → дорожка нодов открытого юнита. Авто-раскрытие к текущему уроку (и после финала урока); прогресс-бары у частей, счётчики у этапов/юнитов, зелёная подсветка завершённых; «Продолжить» по-прежнему ведёт сразу в урок.
- ✅ **Дистракторы** (жалоба Ивана: правильный ответ выделяется, напр. длиннее): теперь подбираются ближайшие по длине и числу слов к правильному ответу (+случайный джиттер), и в вариантах всегда ≥1 уже изученное слово (по SRS seen>0), если такие есть.
- ✅ Пуш index.html (42793 байта) в корень ninenko/ky-app; live проверен curl'ом (метка v0.2-ui+nav1, размер совпал). sw.js network-first — бамп кэша не нужен.
- ⚙️ Тех: синк опять завис на index.html (частично отдавал старые 36094 байта >10 мин) — тот же обход: полный файл → outputs/index_sync.html (новые файлы синкаются сразу) → node --check всего JS OK → cat поверх → пуш.
- ⏳ От Ивана: обновить страницу на телефоне, потыкать подсказки и дерево.

## 2026-07-04 — Аудит переводов (word bank v1.1), микро-уроки, редизайн v0.2-ui, маскот Суур
- 🔍 **Аудит по жалобе Ивана** (карточка «менен» показывала словарную статью): из 850 переводов ~70 — словарный мусор, ~60 — неверное значение (Tilchi брал первую статью Юдахина: «жана»=«опять» вместо «и», «болот»=«сталь» вместо «будет», «пайыз»=«поезд» вместо «процент», «сабак»=«стебель» вместо «урок», «аял»=«промедление» вместо «женщина», «Бишкек»=«мутовка для кумыса» и т.д.), ~150 — «и. д. от…»/«то же, что…» без перевода.
- ✅ `content/curate_translations.py`: 779 переводов откурированы вручную (LLM, Claude), 7 автоочисток. Оригиналы сохранены в `translation_raw`/`translation2_raw`, поле `cleaned`, статус «cleaned (LLM); needs native check» (итого 789 флагов). Схема: `;` в переводе отделяет вторичное значение; приложение показывает часть до `;`. Бэкап words_backup_pre_curation.json. Ручной проверки носителем не будет (решение Ивана 2026-07-04) — флаги остаются в данных, UI ими не пестрит (одна сноска в футере).
- ✅ **Микро-уроки**: build_course.py LESSON_SPLIT [6,6,5] → [3,3,3,3,3,2]; курс = 50 юнитов × 6 уроков = 300 уроков по 2–3 слова (~9–13 заданий, было 24).
- ✅ **App v0.2-ui** (редизайн в духе Duolingo по research/duolingo_playbook_adoptation.md): 3D-кнопки, зигзаг-дорожка уроков с цветными нодами и пульсирующим «НАЧАТЬ», статус-бар (🔥 стрик / ❄️ заморозки / ⚡ XP / кольцо цели дня), кнопка «Продолжить», уровни-титулы (Баштоочу→Манасчы), комбо-счётчик 🔥N, новое упражнение «Соедини пары», фидбек-плашка снизу с «Азамат!», конфетти + экран итогов (XP/точность/стрик), XP: 10 за новый урок + 2 за слово без ошибок + 5 за идеальный. Убраны «(тема: ИИ)» и пер-карточный ⚠. sw.js кэш v2.
- ✅ **Маскот Суур** (гималайский сурок, оригинальный SVG — стоковое фото Ивана не использовано, копирайт): анимации — покачивание, моргание, наклон головы; машет лапой на входе («Салам! Я Суур»), ликует с прыжком на финале урока; в шапке главной.
- ✅ RTDB: `wordbank` (v1.1) и `course` (300 уроков) перезалиты, спот-чек ок. Push в ninenko/ky-app корень: index.html (35155 байт), sw.js; случайный дубль в src/ репо удалён.
- ✅ tracker.xlsx: Translation (rus) обновлён у 715 строк, Status у всех; бэкап tracker_backup_20260704b.xlsx перезаписан (хранится 2).
- ⚙️ Тех: синк Windows↔песочница дважды подвисал на index.html (частичная копия 20002 байта) — обход: полный файл → новый временный → cat поверх; первый пуш ушёл обрезанным, перепушено с проверкой размера. db.py и push.py: /tmp в песочнице недоступен → кэш токена и клон перенаправлены в TMPDIR.
- ✅ **Суур v2** (фидбек Ивана «руки как пятна на животе»): лапы перерисованы (по бокам туловища, тёмные кисти), вращение от плеча (transform-box:view-box + пивоты в px). Анимации ответов: верный вариант — зелёный пульс, неверный — тряска + красная вспышка; в плашке фидбека мини-Суур радуется (happy) или грустит (sad, уши-глаза вниз). Перепушено (36159 байт, проверено через API).
- ✅ **Полировка по скриншоту Ивана**: убран 🇰🇬 после «Научу тебя кыргызскому» (Windows рендерит эмодзи-флаги как буквы «KG»), убран «шов» на лбу Суура (тёмная SVG-шапочка), эмодзи уровней заменены на надёжно рендерящиеся (🛠→🔨, 🏔→🗻, 🎶→🎵), guard на пустую пилюлю темы в интро-карточке. Прогон по всем экранам: login/home/урок (4 типа заданий)/фидбек/финал — других артефактов не найдено. Пуш 36094 байта (проверено через API).
- ⏳ От Ивана: обновить страницу на телефоне (кэш Pages ~10 мин + SW), пройти пару уроков, посмотреть на Суура.

## 2026-07-04 — App v0.1 собрано и запушено; ждём включения Pages Иваном
- ✅ `src/`: index.html (одностраничная PWA), manifest.webmanifest, sw.js (кэш только оболочки), icon-192/512.png (сгенерированы PIL: тюндюк-мотив).
- ✅ Функции: Google-вход (popup→redirect fallback) → загрузка `wordbank`+`course` из RTDB (compat SDK 10.12.2, конфиг из `_quickref.md`) → кэш контента и прогресса в IndexedDB (local-first, синк в RTDB = v0.2). Упражнения: интро-карточка, кыр→рус (выбор), рус→кыр (выбор), набор текста (кнопки ө/ү/ң). SM-2-lite: шаги 1-3-7-16-35-80-180 дней, ошибка = откат на 2 шага + повтор в конце урока. Стрик с 2 заморозками (авто-пополнение раз в 7 дней стрика), цель дня 1–5 уроков, XP. Русский UI, mobile-first. Флаг ⚠ «не проверено носителем» показывается в упражнениях; темы юнитов подписаны «(тема: ИИ)».
- ✅ Проверки: node --check обоих скриптов OK; утечек контента в публичные файлы нет (грепал словарные слова); анонимный REST к RTDB → Permission denied ✔.
- ✅ Push в ninenko/ky-app (корень репо): index.html, manifest, sw.js, 2 иконки.
- ✅ **Pages включён Клодом через Chrome** (по просьбе Ивана): Settings → Pages → main / (root) → Save («source saved»). Через ~15 сек https://ninenko.github.io/ky-app/ → 200, экран входа «Кыргызча» рендерится (скриншот проверен).
- ⏳ От Ивана: открыть URL с телефона → «Войти через Google» → «Add to Home Screen». Первый вход заодно проверит чтение RTDB под его аккаунтом.

## 2026-07-04 — Курс v1 построен и залит в RTDB (узел `course`), «ок» Ивана получен
- ✅ `content/build_course.py` + `content/course.json`: 850 слов → 10 частотных ярусов × 5 юнитов по 17 слов → 50 юнитов, 150 уроков (6+6+5). Семантическая группировка внутри яруса.
- ✅ Все темы назначены LLM и помечены `topic_source: "LLM"` (на каждом слове + note в meta); переводы не тронуты; 15 флагов «needs native check» сохранены (`needs_native_check: true`); 48 омонимов без уверенной темы → «Общая лексика».
- ✅ Сэмпл (список юнитов + юнит 5 целиком) показан Ивану → «Ок, загружай».
- ✅ Залито в RTDB: `db.py set course` → спот-чек meta (units=50, words=850), units/4/title, units/49/words/16 (майрам, ө/ү/ң целы). Аноним REST → Permission denied ✔.
- ✅ tracker.xlsx: колонка Topic заполнена для всех 850 слов с пометкой «(LLM)» (бэкап tracker_backup_20260704b.xlsx; старый 20260703c удалён, хранится 2).
- ⚙️ Тех.заметка: стейл-кэш токена `/tmp/.fbtoken-*` от прошлой сессии не перезаписывался → обход через sitecustomize-редирект (PYTHONPATH), db.py не менялся.
- ⏭️ Дальше: app v0.1 в `src/` → push на Pages.

## 2026-07-04 — Firebase живой: ключ на месте, rules применены, банк слов залит
- ✅ Иван перенёс ключ → переименован в `firebase-connect/.service-account.json`; `db.py test` OK.
- ✅ Rules применены (`rtdb.rules.json`): read/write только `ivan.ninenko@gmail.com`. Проверено: анонимный REST-запрос → Permission denied.
- ✅ **words.json (850 слов) залит в RTDB** → узел `wordbank` (`meta` + `words`). Спот-чек: слова 0 и 849, кириллица/ө-ү-ң целы, count=850.
- ⏭️ Дальше: структура курса из 850 слов → app v0.1 (SRS + упражнения, локальный прогресс) → push на Pages.

## 2026-07-04 — Инфраструктура создана Клодом через Chrome (по «ок» Ивана)
- ✅ **GitHub**: репо `ninenko/ky-app` создан (public, README). Pages включим после первого пуша.
- ✅ **Firebase**: проект `ky-app` (ID: **ky-app-9c14d**), Analytics off. RTDB `europe-west1`, locked mode. Auth: Google включён. Web app `ky-app-web` зарегистрирован. Authorized domains: + `ninenko.github.io`. Ключ сервис-аккаунта сгенерирован → Загрузки Ивана.
- ✅ firebaseConfig и все ID → `_quickref.md` (создан). `firebase-connect/config.env` — реальные PROJECT_ID/DATABASE_URL.
- ✅ Подготовлен `firebase-connect/rtdb.rules.json` (доступ только ivan.ninenko@gmail.com) — применю через `db.py rules-set` после появления ключа.
- ⏳ От Ивана: перенести скачанный JSON → `firebase-connect/.service-account.json`. Затем: `db.py test` → залить words.json в RTDB → app v0.1 → push на Pages.

## 2026-07-04 — Connect-папки от Ивана → стек уточнён (GitHub Pages + RTDB)
- ✅ Иван добавил `github-connect/` (push.py + PAT) и `firebase-connect/` (db.py + сервис-аккаунт, RTDB) — портативные коннекторы из BIS-проекта.
- ✅ Стек уточнён: хостинг = **GitHub Pages** (деплой через push.py, репо `ky-app` — только оболочка, без копирайтного контента); данные = **Realtime Database** вместо Firestore (words.json + прогресс, за Firebase Auth, rules только на email Ивана). CLAUDE.md (Stack) обновлён.
- ✅ `config.env` обоих коннекторов перенастроены с bis-pillars на ky-app (Firebase — placeholder до создания проекта).
- ✅ `src/FIREBASE_SETUP.md` переписан: репо ky-app + Firebase-проект + сервис-ключ → `firebase-connect/.service-account.json`.
- ⏳ Ждём от Ивана: репозиторий `ky-app`, Firebase-проект, firebaseConfig + ключ + Database URL.

## 2026-07-04 — Решение по стеку: PWA + Firebase; housekeeping
- ✅ **Стек утверждён Иваном: PWA + Firebase, не APK.** Прогресс local-first (IndexedDB) + синк в Firestore; хостинг Firebase Hosting за Firebase Auth (только Google-аккаунт Ивана — закрывает копирайтный контент от публики). План: v0.1 приложение + локальный прогресс, v0.2 Auth + синк. Зафиксировано в CLAUDE.md (секция Stack).
- ✅ Инструкция для Ивана: `src/FIREBASE_SETUP.md` (создать проект, Auth, Firestore + rules). Ждём `firebaseConfig` → `_quickref.md`.
- ✅ CLAUDE.md актуализирован: Current state отставал на этап (говорил «tracker empty») — теперь отражает word bank v1 (850), обработанные источники, следующие шаги.
- 🔄 Housekeeping: PDF-сканы Юдахина перенесены `sources/` → `sources/dictionaries/` (пути обновлены в трекере и `_manifest.md`); отработанный `HANDOVER_process_cv_rferl.md` → `.claude/archive/`.

## 2026-07-04 — Word bank v1: 850 слов залито (по «ок» от Ивана)
- ✅ Иван одобрил сэмпл → создан `content/words.json` (**850 слов**): топ-3000 частотных токенов (RFE/RL+CV) × словарь Tilchi. До 2 коротких значений на слово, отсылки «см./то же, что» разрешаются автоматически, счётчик «ещё значений».
- ✅ Скрипт сборки (воспроизводимый): `content/build_wordbank.py`.
- ✅ Tracker → Word bank sheet: 850 строк (пример «Салам» заменён реальными данными). Backup `tracker_backup_20260704.xlsx` (ротация: 2 бэкапа).
- 📊 Статусы: 835 = "auto (Tilchi)" (словарные, выборочно проверены); 15 = "needs native check (LLM)" — служебные слова (ал, бул, мен, эмес…), чьи словарные статьи — мусор; переводы заданы вручную из учебной грамматики.
- ⏭️ Отложено: ~100 токенов с мусорными глоссами пропущены; словоформы без словарной статьи (боюнча-тип) ждут лемматизации; Topic/Type пусты — не выдумываем.
- 📝 Новый файл `content/usage_notes.md` — правки живого употребления от Ивана (первая: киши→адам, подтверждена корпусом 137k vs 82k).

## 2026-07-03 — Common Voice 26.0 + RFE/RL corpus processed → frequency signal
- ✅ Located both archives at project root (not `sources/`), filed them: `sources/audio/cv-corpus-26.0-2026-06-12-ky.tar.gz` (1.04 GB) and `sources/corpora/rferl-kyrgyz-news-corpus-v1.0.tar.gz` (296 MB → `azattyk.ky.txt` 1.2 GB). ө/ү/ң OK in both.
- ✅ **RFE/RL (Azattyk)**: tokenized 339,099 ky articles / **77.7M tokens** → `content/frequency/rferl_ky_frequency_full.tsv` (818,061 types). This is now the **primary modern-vocabulary frequency ranking**. Raw article text NOT copied anywhere — only counts.
- ⚠️ RFE/RL license: handover said CC-BY-NC-SA; the corpus README actually says © RFE/RL Inc. / Terms of Use / credit required. Recorded both in tracker. Either way NC → raw text must never ship in a public app.
- ✅ **Common Voice 26.0 ky** (CC0): registered as audio asset (clips kept inside archive); `content/audio/cv_clip_index.tsv` = 30,741 clip↔sentence rows; transcripts tokenized → `content/frequency/cv_ky_frequency.tsv` (202,593 tokens / 10,624 types, secondary signal).
- ✅ Merged signal → `content/frequency/ky_frequency_merged.tsv` (106,799 rows: token + rferl_count + cv_count + frekwencja_rank; hapax noise dropped at rferl_count<20 unless in CV/frekwencja).
- ✅ Verified: top tokens are real Kyrgyz (менен, жана, боюнча, үчүн…); counts spot-checked by independent recount (grep/py, incl. Керек capitalization case); 10/10 sample rows verified against Tilchi source.
- 📋 **Test-run sample** for Ivan: `content/frequency/SAMPLE_wordbank_candidates.tsv` — top-200 tokens × Tilchi translations (123 matched; 77 are inflected forms without direct headword → need lemmatization later). NOT bulk-ingested into the word bank — awaiting Ivan's "looks right?". Known issue: Tilchi 1st-sense pick is sometimes the wrong sense (e.g. же I «довольно!» vs же «или»).
- 🔄 Tracker: CV row updated + RFE/RL row added, both → Processed (backup `tracker_backup_20260703c.xlsx`; oldest backup rotated out, 2 kept).

## 2026-07-03 — Peace Corps audio+ebooks downloaded; OmniVoice TTS noted
- ✅ Downloaded 6 Peace Corps Kyrgyz audio files (lessons 1–8, ~22 min, verified with ffprobe) from bizdin.kg → `sources/peace_corps_audio/`. Public domain (US gov).
- ✅ Downloaded 3 matching Peace Corps ebooks from Live Lingua → `sources/peace_corps_ebooks/`: Basic Interactive Lessons (18 p., transcripts for the audio), Language Manual (325 p.), Kirghiz Language Competencies (167 p.). All PDFs verified.
- ✅ Tracker: +5 rows on Sources sheet (backup `tracker_backup_20260703.xlsx`).
- 📝 Tip from Ivan's friend: OmniVoice (k2-fsa) TTS "говорит очень прилично на кыргызском" — open source, 600+ langs incl. Kyrgyz, voice cloning. Candidate for word/phrase audio; any generated output needs native-speaker check before main bank.
- Rest of bizdin.kg audio catalog (audiobooks, Grimm tales, komuz, epic anthology) assessed: copyrighted/long-form, low priority for the app.
- 📝 Ivan shared: Common Voice Scripted Speech 26.0 Kyrgyz on Mozilla Data Collective — CC0, 30,741 validated clips / 5,051 sentences with clip↔text TSVs. Added to tracker (Status: New, not downloaded — 1.04 GB, MDC account needed). Best native-audio source found so far; also the designated open asset if the app ever goes public.

## 2026-07-03 — Tilchi .tdb CRACKED + extracted
- ✅ Reverse-engineered the Tilchi `.tdb` format from the open-source repo (IncorexLLC/Tilchi). Encryption = **Blowfish-ECB** (canonical big-endian key schedule + F, but 8-byte blocks read little-endian per x86 file layout); key = 88-byte CP1251 proverb hardcoded in `MainForm.cpp` (first 72 bytes used). Payload = Msftedit RTF (CP1251, charset 204). Kyrgyz ө/ү/ң stored as CP1251 slots є/ї/ў.
- ✅ Decrypted both files → `content/tilchi/KR.rtf`, `content/tilchi/RK.rtf`.
- ✅ Extracted clean headword→definition pairs: **36,891** Ky→Ru (`tilchi_KR.tsv`) + **48,358** Ru→Ky (`tilchi_RK.tsv`). ө/ү/ң verified correct on spot-check.
- ✅ Tooling saved: `content/tilchi/decrypt_tdb.py`, `content/tilchi/extract_tilchi.py`, `content/tilchi/README.md`.
- ⏭️ Next: review sample, then merge into `content/words.json` word bank (test-run rule).
- 🔄 Tracker: Tilchi KR/RK rows → Status = Processed.

## 2026-07-03 — Frequency list + sentence corpora (Batch 1/2)
- ✅ **Kyrgyz frequency list** (frekwencja, 5,050 words) → `sources/frequency/ky-frequency-5050.txt`. Noisy → needs cleaning before tiering.
- ✅ **GoURMET ky-ru** parallel corpus (CC0, **23,016 aligned pairs**) → `sources/corpora/GoURMET-crawled.ky-ru.zip` (+ extracted `_gourmet_peek/`).
- ✅ **Tatoeba Kyrgyz** (702 sentences, CC-BY) → `sources/corpora/kir_sentences.tsv`.
- ✅ Wrote manifests for `frequency/` and `corpora/`; backed up tracker (`tracker_backup_20260703b.xlsx`); logged 3 new Sources rows.
- ℹ️ Yudakhin PDFs (added manually by Ivan) noted in tracker as reference scans.

## 2026-07-03 — Dictionaries downloaded (Batch 1, partial)
- ✅ Wrote `sources/DOWNLOAD_PLAN.md` (3-batch source ingest plan).
- ✅ Downloaded **kaikki.org Kyrgyz** Wiktextract JSONL → `sources/dictionaries/kaikki-Kyrgyz.jsonl` (4,052 entries, CC-BY-SA). ө/ү/ң OK.
- ✅ Downloaded **Tilchi** KR.tdb + RK.tdb (Ky↔Ru, Yudakhin-derived, open-source) → `sources/dictionaries/tilchi/`.
- ⚠️ **Yudakhin StarDict** NOT obtained — tili.kg is bot-protected (HTTP 403); flagged Status=Error, needs manual download. See `sources/dictionaries/_manifest.md`.
- ✅ Backed up tracker → `tracker_backup_20260703.xlsx`; logged 4 rows in Sources sheet; wrote `_manifest.md`.

## 2026-07-03 — Project initialization
- ✅ Deployed project from NEW_PROJECT_TEMPLATE-2.md (app-build-focused structure).
- ✅ Created folders: `research/`, `sources/`, `content/`, `src/`, `.claude/`.
- ✅ Moved 3 docs into `research/`: proposal, Duolingo playbook, resources research.
- ✅ Wrote `CLAUDE.md` (English, to save tokens), `.claude/workflow.md`, this changelog.
- ✅ Built `tracker.xlsx` (Sources + Word bank sheets, empty).
- ✅ Deleted template copy from project folder.
- State: awaiting first sources in `sources/` → Stage 2 (process → word bank).
